# 食衡资料库扩充与项目结构优化补充

> 本文是对《个人日常饮食记录后续开发计划》的补充，只记录后续两次审查中新增的资料库和工程结构结论。

- 日期：2026-09-06
- 适用范围：个人日常使用、Android 优先、离线食品查询
- 目标：扩大日常食品覆盖，同时保证查询正确、数据可更新、记录不会因异常而丢失

---

## 1. 当前判断

目前项目已经完成 SQLite/FTS 食品目录、Repository 抽象、FlatList 搜索分页、持久化迁移和 CI 基础检查。

当前食品库约 4,600 条，其中澳洲官方数据占主体，澳洲包装食品数量仍偏少。对个人日常使用而言，继续增加页面的收益低于以下两件事：

1. 让常买的澳洲超市食品可以按品牌和条码快速找到；
2. 确保数据库、搜索和本机记录在异常情况下不会产生错误或丢失。

---

## 2. 资料库快速扩充方案

### 2.1 第一优先：澳洲包装食品

优先导入 Woolworths、Coles 以及常见亚洲超市中实际会购买的包装食品，包括：

- 早餐谷物、面包、燕麦和饼干；
- 牛奶、酸奶、植物奶和奶酪；
- 罐头、冷冻食品和即食食品；
- 酱油、蚝油、辣椒酱、咖喱酱和汤底；
- 饮料、奶茶、零食和甜品；
- 豆制品、方便面和亚洲调味食品。

最适合使用 Open Food Facts 的澳洲产品数据。批量扩充应在电脑端完成筛选和清洗，然后生成应用内 SQLite 数据库，不建议在手机上逐条联网请求。

Open Food Facts 官方说明：超过几百个产品时应使用 CSV/JSONL 数据下载；搜索 API 有访问频率限制，不适合搜索即输入模式。[API 使用说明](https://openfoodfacts.github.io/openfoodfacts-server/api/)、[数据集使用说明](https://openfoodfacts.github.io/openfoodfacts-python/usage/)

建议第一批增加 500–2,000 条质量较好的澳洲包装食品，并保留：

- GTIN/条码；
- 品牌和产品名；
- 每 100 克或每 100 毫升营养值；
- 包装份量；
- 成分和过敏原（如数据完整）；
- 数据来源、更新时间和可信等级。

Open Food Facts 数据为社区贡献，准确性和完整性不一，必须保留“包装标签优先”的提示，并把缺失营养素与真实的零区分开。

### 2.2 第二优先：用户实际购买的包装标签

对于 Open Food Facts 没有覆盖的食品，先支持表格或表单批量录入，而不是立即建设在线商品库。

最小字段：

- 名称、品牌、条码；
- 每 100 克/毫升营养值；
- 包装份量和单位；
- 购买渠道；
- 标签照片（可选，仅作本人复核）。

这些数据标记为“用户录入/估算”，不与澳洲官方食材自动合并。

### 2.3 第三优先：常吃中餐和亚洲家常菜

优先补 50–100 道真实高频菜，例如炒饭、炒面、粥、汤、饺子、点心、盖饭和常见家庭小炒。

每道菜使用配方模板：

- 食材及克重；
- 用油、酱料和糖；
- 成品重量；
- 默认人数和个人份额；
- 营养计算方式和估算说明。

现有 `dish-compositions` 和合菜份额逻辑可以直接复用。配方估算应与澳洲官方单一食材数据分开标识。

香港食物安全中心的营养查询系统包含点心、粥粉面饭、早餐和中式饮品等资料，可作为人工核对和命名参考；使用前仍需确认具体授权和字段可导入性。[NIIS 食物营养查询](https://www.cfs.gov.hk/english/nutrient/)

### 2.4 第四优先：USDA 补缺

现有流程只使用 USDA Foundation Foods。后续可以加入：

- SR Legacy：更多基础食材和熟制食材；
- FNDDS：膳食调查中的熟食和组合餐。

USDA 数据应作为海外对照，不能替代澳洲官方值，也不能改变澳洲食品的默认来源排序。[USDA 数据下载](https://fdc.nal.usda.gov/download-datasets/)

---

## 3. 资料导入规则

所有新数据进入应用前必须经过以下流程：

1. 原始文件保存到本地数据源目录，并记录下载日期和许可；
2. 转换为统一 Food 模型；
3. 校验能量、单位、份量、条码和必需营养字段；
4. 去重并生成稳定 ID；
5. 区分官方、包装标签、海外对照、用户录入和配方估算；
6. 生成中英文名称、别名和搜索字段；
7. 重新生成 SQLite/FTS 数据库；
8. 运行目录质量、搜索结果和数据库完整性测试。

数据库版本必须随内容变化递增。建议保存源数据哈希、食品总数、各来源数量和生成时间，避免源文件已更新而应用仍使用旧数据库。

---

## 4. 项目结构中需要立即处理的问题

### P0：防止本机记录被异常覆盖

[nutrition-store.tsx](D:/health/mobile/src/store/nutrition-store.tsx) 在读取 AsyncStorage 失败后仍可能进入“已加载”状态，随后把空状态写回原存储。

处理要求：

- 区分“没有历史数据”和“读取失败”；
- 读取失败时暂停自动保存；
- 显示恢复提示，并保留当前存储内容；
- 只有成功读取或确认首次使用后才允许写入。

### P0：修复自定义食品导致官方来源遗漏

[sqlite-food-repository.ts](D:/health/mobile/src/data/sqlite-food-repository.ts) 中，存在任意自定义食品时，官方食品的聚类查询可能返回不到当前官方条目。

影响：食品详情页可能无法显示正确的默认来源、来源切换和营养数据。

处理要求：

- 自定义食品只作为额外来源加入；
- 官方食品查询必须始终保留自身和完整官方聚类；
- 增加“有自定义食品时查询官方牛奶/苹果/鸡蛋”等回归测试。

### P0：确保 SQLite 数据库进入版本管理

必须提交：

- `assets/catalog/foods.db`；
- `assets/catalog/README.md`；
- SQLite Repository、平台 Provider、Metro 配置和数据库生成脚本。

否则干净检出后 CI 或 Android 构建会找不到数据库资产。

### P1：数据库新鲜度检查

当前 CI 会读取数据库并测试部分样例，但不会证明数据库与 TypeScript 数据源一致。

建议在 CI 中增加以下任一机制：

- 自动运行 `catalog:build`，然后检查生成结果；
- 比较源数据哈希和数据库内的构建元数据；
- 校验食品总数、各来源数量、版本号和更新时间。

扩库流程应固定为：

```text
导入原始数据 → 清洗和校验 → 生成数据库 → 目录测试 → CI 检查 → 提交数据库资产
```

### P1：搜索候选截断和排序

SQLite 搜索目前最多先取 200 个候选，再执行分类、来源过滤和相关性排序。关键词较宽时，后面的正确结果可能在排序前被截掉。

后续优化方向：

- 尽量把来源和分类过滤放入 SQL；
- FTS 查询增加明确排序；
- 对条码、品牌和精确中文名设置高优先级；
- 对模糊搜索使用可解释的分页游标；
- 增加“milk、牛奶、酱油、方便面”等宽关键词测试。

### P1：避免同步查询阻塞界面

原生 Repository 使用 Expo SQLite 同步查询。当前候选数量较小，个人使用可以接受，但需要在中端 Android 真机测量：

- 搜索响应 p50/p95；
- 首次打开搜索页耗时；
- 连续输入时掉帧情况；
- 首次复制数据库耗时。

如果搜索 p95 超过约 100ms，再改为更小候选集、异步查询或后台处理。

### P2：拆分全局状态

Nutrition Context 仍同时管理日记、资料、收藏、自定义食品和菜谱。食品目录已移出后，当前影响下降，但加入趋势分析和编辑功能前仍应拆分，避免任何一条记录变化导致所有页面重算。

建议拆为：

- 日记记录；
- 用户资料和目标；
- 收藏、自定义食品和核验；
- 家庭菜谱；
- 备份、恢复和启动状态。

---

## 5. 推荐执行顺序

### 第一轮：修复可靠性

1. 修复读取失败后可能覆盖数据的问题；
2. 修复自定义食品导致官方来源遗漏的问题；
3. 增加回归测试；
4. 将 SQLite 资产和全部新增文件加入 Git；
5. 在干净环境重新运行 CI。

### 第二轮：扩大高频食品

1. 筛选 Open Food Facts 澳洲包装食品；
2. 导入第一批 500–2,000 条；
3. 补充个人实际购买但未命中的标签食品；
4. 加入 50–100 道常吃中餐配方；
5. 重新生成数据库并更新目录版本。

### 第三轮：优化搜索和长期维护

1. 增加数据库新鲜度和哈希检查；
2. 修复候选截断和宽关键词排序；
3. 完成 Android 真机性能测试；
4. 根据真实使用数据决定是否拆分 Nutrition Context；
5. 再考虑条码扫描，而不是先建设在线商品服务。

## 6. 暂不投入

个人版本暂不需要为以下事项增加结构复杂度：

- 云端商品库；
- 用户账号和同步；
- 在线审核后台；
- 订阅或商业化数据接口；
- 大规模图片识别服务；
- 全量 Web 食品库优化。

只要本地目录覆盖常吃食品、查询可信、记录可恢复，个人日常使用就已经能获得主要价值。
