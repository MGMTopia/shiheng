# 食衡资料库扩充与项目结构优化补充

> 本文是对《个人日常饮食记录后续开发计划》的补充，只记录后续两次审查中新增的资料库和工程结构结论。

- 日期：2026-09-06
- 适用范围：个人日常使用、Android 优先、离线食品查询
- 目标：尽可能覆盖个人日常饮食场景，同时保证查询正确、数据可更新、记录不会因异常而丢失

---

## 1. 当前判断

目前项目已经完成 SQLite/FTS 食品目录、Repository 抽象、FlatList 搜索分页、持久化迁移和 CI 基础检查。

当前食品库约 4,600 条，其中澳洲官方数据占主体，澳洲包装食品数量仍偏少。对个人日常使用而言，资料库的目标不是单纯追求条目数量，而是覆盖本人会遇到的不同记录场景：

1. 家庭烹饪的单一食材和合菜；
2. Woolworths、Coles 和亚洲超市的品牌包装食品；
3. 外卖、餐馆、饮料、零食和临时购买食品；
4. 不同包装规格、份量、品牌和条码的独立营养标签；
5. 离线查询、历史补记、复制餐食、修改和恢复。

对个人版本而言，覆盖这些场景比建设账号、云端商品库或复杂推荐系统更有价值。

## 2. 个人日常场景覆盖

资料库和记录流程应至少覆盖以下场景：

| 场景 | 需要的数据或功能 | 记录方式 |
|---|---|---|
| 早餐 | 面包、牛奶、燕麦、谷物、咖啡、奶茶、鸡蛋 | 最近使用、常见份量 |
| 家庭午晚餐 | 米饭、面、蔬菜、肉、汤、家常小炒 | 单食材或家庭配方 |
| 合菜分食 | 一桌多道菜、不同人数、个人夹取份额 | 共享人数 + 个人份额 |
| 外卖和餐馆 | 盖饭、炒饭、面、点心、快餐 | 配方估算 + 用油等级 |
| 包装食品 | 早餐谷物、酸奶、酱料、罐头、零食 | 条码 + 独立标签 |
| 饮品 | 奶茶、咖啡、含糖饮料、植物奶 | 毫升、糖和份量 |
| 临时食品 | 没有底库的产品或菜品 | 自定义食品 + 标签照片 |
| 历史补记 | 昨天或更早的漏记 | 日期、餐次、复制记录 |
| 剩菜和重复餐 | 前一天相同菜品或套餐 | 复制上一餐并调整份量 |
| 数据不完整 | 标签缺字段、估算菜、未知份量 | 显示未知，不伪造为零 |

每个场景都应能完成“找到或创建 → 选择份量 → 记录 → 查看营养”的闭环。

---

## 3. 资料库快速扩充方案

### 3.1 第一优先：澳洲包装食品

优先导入 Woolworths、Coles 以及常见亚洲超市中实际会购买的包装食品，包括：

- 早餐谷物、面包、燕麦和饼干；
- 牛奶、酸奶、植物奶和奶酪；
- 罐头、冷冻食品和即食食品；
- 酱油、蚝油、辣椒酱、咖喱酱和汤底；
- 饮料、奶茶、零食和甜品；
- 豆制品、方便面和亚洲调味食品。

最适合使用 Open Food Facts 的澳洲产品数据。批量扩充应在电脑端完成筛选和清洗，然后生成应用内 SQLite 数据库，不建议在手机上逐条联网请求。

Open Food Facts 官方说明：超过几百个产品时应使用 CSV/JSONL 数据下载；搜索 API 有访问频率限制，不适合搜索即输入模式。[API 使用说明](https://openfoodfacts.github.io/openfoodfacts-server/api/)、[数据集使用说明](https://openfoodfacts.github.io/openfoodfacts-python/usage/)

建议第一批增加 500–2,000 条质量较好的澳洲包装食品，并保留：

- GTIN/条码；
- 品牌和产品名；
- 每 100 克或每 100 毫升营养值；
- 包装份量；
- 成分和过敏原（如数据完整）；
- 数据来源、更新时间和可信等级。

每一个品牌产品必须有独立记录和独立营养成分表。不能因为产品名相同、分类相同或营养值接近，就把不同品牌或不同规格合并成一个可记录条目。

建议使用稳定的产品 ID：

```text
au:gtin:<条码>
```

没有 GTIN 时，使用来源加来源编号的组合 ID，并标记为待核验。产品换包装或营养配方改变时，保留版本日期，不直接覆盖历史产品。

包装产品至少需要以下字段：

- GTIN/条码；
- 品牌方、品牌和产品名称；
- 口味、规格和包装净含量；
- 每 100g/100ml 营养值；
- 每份营养值、份量单位和每包装份数；
- 成分表、过敏原和标签照片（有则保存）；
- 来源、采集日期、数据版本和产品状态。

Open Food Facts 数据为社区贡献，准确性和完整性不一，必须保留“包装标签优先”的提示，并把缺失营养素与真实的零区分开。

### 3.2 第二优先：用户实际购买的包装标签

对于 Open Food Facts 没有覆盖的食品，先支持表格或表单批量录入，而不是立即建设在线商品库。

最小字段：

- 名称、品牌、条码；
- 每 100 克/毫升营养值；
- 包装份量和单位；
- 购买渠道；
- 标签照片（可选，仅作本人复核）。

这些数据标记为“用户录入/估算”，不与澳洲官方食材自动合并。

### 3.3 第三优先：常吃中餐和亚洲家常菜

优先补 50–100 道真实高频菜，例如炒饭、炒面、粥、汤、饺子、点心、盖饭和常见家庭小炒。

每道菜使用配方模板：

- 食材及克重；
- 用油、酱料和糖；
- 成品重量；
- 默认人数和个人份额；
- 营养计算方式和估算说明。

现有 `dish-compositions` 和合菜份额逻辑可以直接复用。配方估算应与澳洲官方单一食材数据分开标识。

香港食物安全中心的营养查询系统包含点心、粥粉面饭、早餐和中式饮品等资料，可作为人工核对和命名参考；使用前仍需确认具体授权和字段可导入性。[NIIS 食物营养查询](https://www.cfs.gov.hk/english/nutrient/)

### 3.4 第四优先：USDA 补缺

现有流程只使用 USDA Foundation Foods。后续可以加入：

- SR Legacy：更多基础食材和熟制食材；
- FNDDS：膳食调查中的熟食和组合餐。

USDA 数据应作为海外对照，不能替代澳洲官方值，也不能改变澳洲食品的默认来源排序。[USDA 数据下载](https://fdc.nal.usda.gov/download-datasets/)

---

## 4. 产品模型和营养标签规则

现有 `Food` 模型适合通用食物，但品牌包装食品需要增加产品层信息。建议在不影响现有日记结构的前提下，逐步补充：

```text
Product
├── identity: gtin, brandOwner, brand, productName, variant, packageSize
├── serving: servingSize, servingUnit, servingsPerPack
├── nutrition: per100g/per100ml, perServing
├── label: ingredients, allergens, image
└── provenance: dataset, sourceId, capturedAt, validFrom, status
```

营养数据需要遵循以下规则：

- 固体使用每 100g，液体使用每 100ml；
- 同时保存标签上的每份值和每 100g/100ml 值；
- 保存 kJ，kcal 作为展示和计算辅助；
- 缺失值使用 `null`，真实零值才使用 0；
- 标签数据、官方数据库、用户录入和家常菜估算使用不同来源等级；
- 同一 GTIN 的新标签可作为新版本，旧版本继续服务历史记录；
- 不同 GTIN 即使名称相同，也不能覆盖或合并为同一记录。

现有聚类功能只能用于“相关食品展示”。记录时必须保存用户实际选择的产品 ID，避免把品牌营养差异抹平。

## 5. 资料导入规则

所有新数据进入应用前必须经过以下流程：

1. 原始文件保存到本地数据源目录，并记录下载日期和许可；
2. 转换为统一 Food 模型；
3. 校验能量、单位、份量、条码和必需营养字段；
4. 去重并生成稳定 ID；
5. 区分官方、包装标签、海外对照、用户录入和配方估算；
6. 生成中英文名称、别名和搜索字段；
7. 重新生成 SQLite/FTS 数据库；
8. 运行目录质量、搜索结果和数据库完整性测试。

数据库版本必须随内容变化递增。建议保存源数据哈希、食品总数、各来源数量和生成时间，避免源文件已更新而应用仍使用旧数据库。

---

## 6. 项目结构中需要立即处理的问题

### P0：防止本机记录被异常覆盖

[nutrition-store.tsx](D:/health/mobile/src/store/nutrition-store.tsx) 在读取 AsyncStorage 失败后仍可能进入“已加载”状态，随后把空状态写回原存储。

处理要求：

- 区分“没有历史数据”和“读取失败”；
- 读取失败时暂停自动保存；
- 显示恢复提示，并保留当前存储内容；
- 只有成功读取或确认首次使用后才允许写入。

### P0：修复自定义食品导致官方来源遗漏

[sqlite-food-repository.ts](D:/health/mobile/src/data/sqlite-food-repository.ts) 中，存在任意自定义食品时，官方食品的聚类查询可能返回不到当前官方条目。

影响：食品详情页可能无法显示正确的默认来源、来源切换和营养数据。

处理要求：

- 自定义食品只作为额外来源加入；
- 官方食品查询必须始终保留自身和完整官方聚类；
- 增加“有自定义食品时查询官方牛奶/苹果/鸡蛋”等回归测试。

### P0：确保 SQLite 数据库进入版本管理

必须提交：

- `assets/catalog/foods.db`；
- `assets/catalog/README.md`；
- SQLite Repository、平台 Provider、Metro 配置和数据库生成脚本。

否则干净检出后 CI 或 Android 构建会找不到数据库资产。

### P1：数据库新鲜度检查

当前 CI 会读取数据库并测试部分样例，但不会证明数据库与 TypeScript 数据源一致。

建议在 CI 中增加以下任一机制：

- 自动运行 `catalog:build`，然后检查生成结果；
- 比较源数据哈希和数据库内的构建元数据；
- 校验食品总数、各来源数量、版本号和更新时间。

扩库流程应固定为：

```text
导入原始数据 → 清洗和校验 → 生成数据库 → 目录测试 → CI 检查 → 提交数据库资产
```

### P1：搜索候选截断和排序

SQLite 搜索目前最多先取 200 个候选，再执行分类、来源过滤和相关性排序。关键词较宽时，后面的正确结果可能在排序前被截掉。

后续优化方向：

- 尽量把来源和分类过滤放入 SQL；
- FTS 查询增加明确排序；
- 对条码、品牌和精确中文名设置高优先级；
- 对模糊搜索使用可解释的分页游标；
- 增加“milk、牛奶、酱油、方便面”等宽关键词测试。

### P1：避免同步查询阻塞界面

原生 Repository 使用 Expo SQLite 同步查询。当前候选数量较小，个人使用可以接受，但需要在中端 Android 真机测量：

- 搜索响应 p50/p95；
- 首次打开搜索页耗时；
- 连续输入时掉帧情况；
- 首次复制数据库耗时。

如果搜索 p95 超过约 100ms，再改为更小候选集、异步查询或后台处理。

### P2：拆分全局状态

Nutrition Context 仍同时管理日记、资料、收藏、自定义食品和菜谱。食品目录已移出后，当前影响下降，但加入趋势分析和编辑功能前仍应拆分，避免任何一条记录变化导致所有页面重算。

建议拆为：

- 日记记录；
- 用户资料和目标；
- 收藏、自定义食品和核验；
- 家庭菜谱；
- 备份、恢复和启动状态。

---

## 7. 推荐执行顺序

### 第一轮：修复可靠性

1. 修复读取失败后可能覆盖数据的问题；
2. 修复自定义食品导致官方来源遗漏的问题；
3. 增加回归测试；
4. 将 SQLite 资产和全部新增文件加入 Git；
5. 在干净环境重新运行 CI。

### 第二轮：建立产品导入能力

1. 扩展产品字段和营养标签版本；
2. 建立 CSV/JSON 标签导入器；
3. 筛选 Open Food Facts 澳洲包装食品；
4. 导入第一批 500–2,000 条，每个 GTIN 独立保存；
5. 补充个人实际购买但未命中的标签食品；
6. 重新生成数据库并更新目录版本。

### 第三轮：补齐日常场景

1. 增加早餐、饮料、零食和外卖常用条目；
2. 加入 50–100 道常吃中餐配方；
3. 支持无底库食品的自定义标签录入；
4. 支持历史补记、复制上一餐和剩菜调整；
5. 增加每份与每 100g/100ml 的营养切换。

### 第四轮：优化搜索和长期维护

1. 增加数据库新鲜度和哈希检查；
2. 修复候选截断和宽关键词排序；
3. 完成 Android 真机性能测试；
4. 根据真实使用数据决定是否拆分 Nutrition Context；
5. 再考虑条码扫描，而不是先建设在线商品服务。

## 8. 暂不投入

个人版本暂不需要为以下事项增加结构复杂度：

- 云端商品库；
- 用户账号和同步；
- 在线审核后台；
- 订阅或商业化数据接口；
- 大规模图片识别服务；
- 全量 Web 食品库优化。

只要本地目录覆盖常吃食品、查询可信、记录可恢复，个人日常使用就已经能获得主要价值。
