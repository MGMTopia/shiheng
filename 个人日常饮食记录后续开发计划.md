# 食衡：个人日常饮食记录后续开发计划

> 面向单人长期自用，以快速记录饮食、查询食物热量和营养成分、观察饮食结构并获得一般性改善建议为目标。

- 版本：1.0
- 日期：2026-09-06
- 适用范围：Android 优先，兼顾 iOS；Web 仅作为辅助入口
- 产品阶段：非商业、无账号、离线优先、单人使用
- 健康边界：用于一般饮食记录和营养教育，不用于疾病诊断、治疗、临床营养管理或替代医生/营养师意见

---

## 1. 当前目标

把食衡做成一个可以每天自然使用、记录负担足够低、数据来源清楚、能持续帮助调整饮食结构的个人工具。

核心闭环：

1. 快速找到食物或常吃套餐；
2. 用合理份量记录一餐；
3. 当天查看能量和主要营养素；
4. 每周发现饮食结构偏差；
5. 获得简单、可执行、非医疗化的下一步建议；
6. 数据始终留在本人设备并可主动备份、恢复和导出。

## 2. 成功标准

个人版本不以注册数、收入或市场份额衡量，而以实际使用体验衡量：

- 普通常吃食物在 10 秒内完成记录；
- 一顿家常合菜在 30 秒内完成记录；
- 中英文名称、常用俗称、品牌或条码能找到大部分日常食物；
- 能查看每份和每 100 克的热量、蛋白质、脂肪、碳水、糖、膳食纤维和钠；
- 能明确区分澳洲官方数据、包装标签、海外对照和配方估算；
- 能修改误记的份量、餐次、日期和食物；
- 能查看当天、过去 7 天和过去 30 天的饮食趋势；
- 能手动导出并完整恢复记录、自定义食品、收藏和菜谱；
- Android 中端设备冷启动和搜索过程无明显卡顿；
- 无网络时，核心查询、记录和分析仍可正常使用。

## 3. 产品原则

### 3.1 优先减少记录负担

记录越精确，通常操作越繁琐。个人版本优先提供合理默认值、最近使用、常吃套餐和快速份量选项，再允许用户按需细调。

### 3.2 展示趋势，不制造虚假精确

家常菜、餐馆菜和合菜只能估算。界面需要持续显示来源和可信度，不把估算值包装成精确检测结果。

### 3.3 建议必须具体且克制

建议优先表达为“下一餐可以增加一份蔬菜”“本周多天钠偏高，可减少高钠酱料”，避免诊断、恐吓或自动制定治疗方案。

### 3.4 离线优先，用户掌握数据

核心功能不依赖账号和服务器。数据默认只留在本机，同时必须允许用户主动备份、恢复和删除。

### 3.5 先完成高频闭环，再加入智能输入

条码、OCR、拍照和语音可以减少输入，但不能替代可靠的编辑、记录、统计和备份能力。

## 4. 当前基础

现有版本已经具备：

- 中英文食品搜索；
- FSANZ AUSNUT/AFCD、USDA 对照及少量包装食品数据；
- SQLite/FTS 原生离线食品目录；
- 份量、餐次、用油和合菜个人份额记录；
- 今日营养概览、日志、收藏、自定义食品和家庭菜谱；
- 数据来源与可信度展示；
- 本地状态恢复、迁移和串行持久化；
- 搜索防抖、分页和虚拟列表；
- 类型检查、代码规范、领域测试和 Web 导出检查。

现阶段的主要缺口不是基础页面数量，而是个人长期使用所需的数据安全、记录修正、历史趋势和更快复用。

## 5. 开发优先级

## P0：长期自用必须可靠

### 5.1 食品数据库可重复生成

- 将 `assets/catalog/foods.db` 纳入版本管理；
- 数据导入后自动生成 SQLite 目录；
- 保存源数据哈希、数据库版本、生成时间和食品数量；
- CI 检查数据库是否与当前源数据和聚类逻辑一致；
- 目录升级后清理设备上的旧版本数据库，避免每次升级残留约 8 MB 文件。

验收标准：在全新检出的代码中，不依赖本机已有文件即可通过测试并完成 Android 打包。

### 5.2 手动备份、恢复和导出

当前 Android 关闭系统备份，卸载或清除数据后记录不可恢复。个人长期使用前必须加入：

- 完整 JSON 备份；
- 从 JSON 恢复并执行版本迁移；
- CSV 饮食日志导出，方便自行分析；
- 导出前显示包含的数据类型和日期范围；
- 恢复前生成安全检查结果，并允许取消；
- 设置页显示最近一次备份时间。

验收标准：在一台设备导出后，可在全新安装中恢复记录、资料、收藏、自定义食品和家庭菜谱，恢复前后核心统计一致。

### 5.3 可以修正记录

- 编辑份量、餐次、日期、时间和个人份额；
- 替换误选食物；
- 删除操作提供短时间撤销；
- 支持补记昨天或更早日期；
- 清除全部数据需要二次确认并提示先备份。

验收标准：任何错误记录都不需要“删除后重新从头录入”。

### 5.4 核心数据可靠性

- 完整校验恢复后的自定义食品、营养字段、类别、来源和标签；
- 明确区分“营养素为零”和“没有数据”；
- 增加数据库完整性、食品数量和典型搜索测试；
- 对存储失败、数据库打不开和备份文件损坏提供可理解的错误提示。

## P1：让每天记录足够轻松

### 5.5 最近使用与常吃

- 首页显示最近食物、最常吃食物和最近套餐；
- 支持固定 6–12 个个人快捷项；
- 快捷项可直接使用上次份量和餐次；
- 支持从昨天或任意历史日期复制一餐；
- 家庭菜谱可以保存默认人数和个人份额。

### 5.6 更自然的份量输入

- 同时支持“份”“克”“毫升”和常见单位；
- 显示单位换算结果，避免用户不知道一份是多少；
- 提供 0.5、1、1.5、2 份快捷选择；
- 对家常菜保留少油、正常、偏油和个人份额设置；
- 记住每种食物最近一次使用的份量。

### 5.7 更完整的营养成分表

食物详情优先展示：

- 能量：kJ 与 kcal；
- 蛋白质、脂肪、饱和脂肪；
- 碳水化合物、糖；
- 膳食纤维、钠；
- 每份与每 100 克切换；
- 数据来源、更新时间、地区和可信度；
- 缺失营养素显示“暂无数据”，不能显示为 0。

### 5.8 条码作为快捷入口

- 优先支持扫描已有本地包装食品；
- 未命中时允许按包装营养标签创建自定义食品；
- 保存条码，下一次可以直接找到；
- 暂不建设服务器商品库和用户审核系统。

## P1：帮助优化饮食结构

### 5.9 今日结构概览

除热量外，首页需要一眼看出：

- 蛋白质是否明显不足；
- 膳食纤维是否不足；
- 钠是否偏高；
- 蔬菜、水果、谷物和蛋白质类食物的大致份数；
- 数据不完整时明确提示统计仅为估算。

不要用单一总分评价一天饮食，避免用户为了“满分”而机械进食。

### 5.10 周趋势

- 最近 7 天和 30 天平均能量；
- 蛋白质、纤维和钠趋势；
- 各食物组出现频率；
- 记录完整度，例如一周记录了几天、每天几餐；
- 可切换只统计“记录较完整”的日期；
- 高钠、低纤维或蔬菜不足等重复模式。

### 5.11 可执行建议

建议按优先级每次只显示 1–3 条：

- 下一餐可以补什么；
- 哪类食物最近出现过少；
- 哪类高钠调料或加工食品出现较多；
- 可以用什么常见食物替换，而不是只说“少吃”；
- 建议附简短原因和依据，但不进行疾病推断。

验收标准：用户看完建议后能立即知道下一餐可以做什么改变。

## P2：有明显日常价值时再做

- 营养标签 OCR；
- 语音输入，例如“晚饭一碗米饭、半份番茄炒蛋”；
- 拍照辅助识别餐食；
- 体重、腰围和个人备注趋势；
- Apple Health / Health Connect 的体重或活动数据读取；
- 个人自定义营养目标模板；
- Web 端食品目录分片、IndexedDB 或 Worker 优化。

这些功能需要在手动记录闭环稳定、备份可靠后再安排。

## 6. 暂不开发

为保持个人工具简单，近期明确不做：

- 注册、登录和云账号；
- 多设备实时同步；
- 订阅、广告和付费；
- 营养师工作台和机构管理；
- 社区、排行榜、连续打卡压力和社交分享；
- 用户增长、推荐邀请和商业分析埋点；
- 在线商品审核平台；
- 餐厅或超市商业合作；
- 医疗诊断、疾病饮食处方和用药建议；
- 为商业发布而进行的大规模品牌、营销和后台建设。

## 7. 技术演进计划

### 7.1 数据层

- 保留 `FoodRepository` 作为食品目录唯一入口；
- Android/iOS 使用 SQLite + FTS；
- Web 暂时保留内存实现；
- 日记记录增长后迁移到用户 SQLite 数据库；
- 食品官方目录与个人数据使用不同数据库，方便升级目录而不影响用户记录。

### 7.2 状态层

短期可以继续使用现有 Context，但新增历史趋势和编辑功能前拆分为：

- 用户资料与目标；
- 日记记录；
- 自定义食品、收藏和核验；
- 家庭菜谱；
- 启动、恢复和备份状态。

页面只订阅自己需要的数据，避免添加一条记录导致所有标签页重新计算。

### 7.3 性能目标

- 中端 Android 冷启动：核心界面 2 秒左右可操作；
- 已打开应用后的常见搜索：p95 小于 100ms；
- 输入搜索时保持流畅，不因 SQLite 同步查询明显掉帧；
- 搜索结果继续使用虚拟列表；
- Android JS 包不重新引入完整 TypeScript 食品目录；
- Web 不是近期主平台，不以牺牲移动端进度为代价压缩 Web 包。

### 7.4 测试重点

- 食品数据库与源数据一致性；
- 状态版本迁移；
- 备份和恢复往返一致性；
- 编辑、删除、撤销和跨日期记录；
- 每日和每周统计；
- 食物组及营养建议边界；
- SQLite 与 Web 内存 Repository 的关键搜索结果一致性；
- Android 真机启动、搜索和长期数据量性能。

## 8. 分阶段执行

### 阶段 A：可靠个人数据

目标：可以放心开始长期记录。

- 提交并校验 SQLite 食品数据库；
- 完成 JSON 备份与恢复；
- 完成 CSV 日志导出；
- 支持修改和补记记录；
- 完成数据损坏及恢复测试。

完成标志：卸载重装后可以通过备份恢复，错误记录可以直接修正。

### 阶段 B：快速日常记录

目标：每天记录不会明显打断生活。

- 最近使用和个人快捷项；
- 上次份量记忆；
- 快速复制昨天或历史餐次；
- 家庭套餐默认人数与份额；
- 本地条码扫描和自定义食品衔接。

完成标志：常见早餐和午餐约 10 秒完成，家常晚餐约 30 秒完成。

### 阶段 C：饮食结构反馈

目标：记录结果能够指导下一餐和下一周。

- 今日食物组结构；
- 7 天与 30 天趋势；
- 记录完整度；
- 重复偏差识别；
- 1–3 条可执行建议；
- 周报 CSV 或简洁摘要导出。

完成标志：不查看原始日志，也能知道最近最值得改善的一两个饮食习惯。

### 阶段 D：选择性提升输入效率

目标：只加入真正能节省本人时间的智能功能。

- 根据实际使用痛点选择 OCR、语音或拍照中的一个；
- 先做本地确认流程，识别结果不能直接写入日志；
- 对比手动输入，只有明显节省时间才保留。

## 9. 下一轮建议任务

按执行顺序：

1. 建立食品数据库版本和新鲜度检查；
2. 将 SQLite 资产及相关新文件完整纳入版本管理；
3. 设计个人数据备份文件格式；
4. 实现 JSON 导出、预览、恢复和迁移测试；
5. 增加日志编辑和跨日期补记；
6. 增加最近使用、个人快捷项和份量记忆；
7. 收集至少连续 7 天的本人真实使用数据；
8. 再根据实际记录摩擦决定周趋势和条码功能的具体顺序。

## 10. 阶段性完成定义

当满足以下条件时，可以认为“个人日常版 1.0”完成：

- 连续使用 30 天没有因为记录太慢而频繁放弃；
- 常吃食品和套餐能够快速复用；
- 营养数据来源和估算边界清晰；
- 可以修正、补记、删除和恢复记录；
- 可以完整备份并在新安装中恢复；
- 可以查看日、周、月的主要营养和食物组趋势；
- 建议能转化为下一餐的实际选择；
- 无网络时核心功能可用；
- 中端 Android 设备没有明显启动和搜索卡顿；
- 不需要账号、服务器或付费服务也能长期使用。
