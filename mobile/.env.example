# Copy to .env for local EAS Update wiring (do not commit .env).
# After `eas init` or `eas update:configure`, set the Expo project ID.
# Channels: preview (closed-trial APK) | production (store).
EAS_PROJECT_ID=
EAS_UPDATE_CHANNEL=preview
